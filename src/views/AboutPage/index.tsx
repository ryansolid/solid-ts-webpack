import styles from './_style.module.scss';

export default () => {
    return (
        <div class={styles.aboutPage}>
            <p class={styles.aboutPageText}>This is an about page</p>
        </div>
    );
};
